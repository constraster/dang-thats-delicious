extends layout

block content
    .inner
        h2= title
        .map
            .autocomplete
                input.autocmplete__input(type="text" placeholders="Search for Anything" name="geolocate")
            #map
                p Loading Map...